<Window x:Class="TempControl.Views.ExportHeadingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export PDF"
        Width="420"
        Height="180"
        MinWidth="380"
        MinHeight="160"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Padding="18" Background="#F5F7FA">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Export PDF"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#1A237E"
                       Margin="0,0,0,12"/>

            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                <TextBlock Text="Report Heading"
                           Margin="0,0,0,6"
                           Foreground="#333"
                           FontWeight="SemiBold"/>
                <TextBox x:Name="HeadingTextBox"
                         Width="340"
                         Padding="8"
                         Text="Temperature Trend Report"
                         KeyDown="HeadingTextBox_KeyDown"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Width="90"
                        Margin="0,0,8,0"
                        IsCancel="True"/>
                <Button Content="Export"
                        Width="90"
                        IsDefault="True"
                        Click="ExportButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
